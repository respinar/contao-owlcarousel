{
  "name": "contao-owlcarousel",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cp -r node_modules/owl.carousel/dist/chart.umd.js public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "owl.carousel": "^2.3.4"
  }
}
