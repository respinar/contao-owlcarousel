{
  "name": "contao-owlcarousel",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cp -r node_modules/owl.carousel/dist/* public/ && cp -r node_modules/animate.css/animate.min.css public/animate/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "animate.css": "^4.1.1",
    "owl.carousel": "^2.3.4"
  }
}
